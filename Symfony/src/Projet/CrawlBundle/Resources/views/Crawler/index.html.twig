{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Crawler list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Url</th>
                <th>Nom</th>
                <th>Selector</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('crawler_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.url }}</td>
                <td>{{ entity.nom }}</td>
                <td>{{ entity.selector }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('crawler_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('crawler_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('crawler_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
